to clone a repo with submodules you should use the `--recursive` flag:

    git clone --recursive <repourl>

and if you forgot to use it the first time you can:

    git submodule update --init

afterwards
